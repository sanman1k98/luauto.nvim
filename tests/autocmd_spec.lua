
local helpers = require "tests.helpers"

local api = helpers.api
local ok = helpers.ok
local not_ok = helpers.not_ok
local truthy = helpers.truthy
local falsy = helpers.falsy
local eq = helpers.eq
local same = helpers.same



describe("a table to manage autocmds", function()
  describe("has a function", function()
    pending("'create'", function()
    end)

    pending("'clear'", function()
    end)

    pending("'get'", function()
    end)

    pending("'del'", function()
    end)

    pending("'exec'", function()
    end)

    pending("''", function()
    end)
  end)

  describe("has a read-only attribute", function()
    pending("'scope'", function()
    end)

    pending("'on'", function()
    end)
  end)

  describe("can", function()
    pending("create them", function()
    end)
    
    pending("clear those matching some criteria", function()
    end)
    
    pending("execute all those for a given event or events", function()
    end)
    
    pending("get all those matching some criteria", function()
    end)
    
    pending("index a table by event name in one of its fields", function()
    end)
    
    pending("delete them one at a time", function()
    end)
  end)

  describe("has a table of events", function()
    pending("in one of its fields", function()
    end)
  end)

  describe("cannot", function()
    pending("manage those outside its scope", function()
    end)
  end)

end)
